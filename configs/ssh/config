# Forward xdg-open socket to remote machines
# %r = remote username (expands to $USER on remote machine)
Host *
    RemoteForward /tmp/open-forward-%r.sock /tmp/open-forward.sock

# static IP set from router
Host streambox
    HostName 192.168.1.250
