.vscode
.ansible
scratchpads

# claude code
.claude

# simpler to put all repos clonsed for inspiration in a combined sub-dir
third-party

# dont commit the git repos for yazi packages installed via `ya pkg`
configs/yazi/plugins
configs/yazi/bookmark
configs/yazi/flavors

# dont commit nvim injections added by tree-sitter-language-injection plugin
configs/nvim/after/queries

# sheldon lock embeds absolute $HOME paths, regenerated per machine
configs/sheldon/plugins.lock

# Devenv
.devenv*
devenv.local.nix
devenv.local.yaml

# direnv
.direnv

# pre-commit
.pre-commit-config.yaml

# env args
.env
